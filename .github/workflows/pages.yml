name: Deploy GitHub Pages

on:
  push:
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    name: Build and deploy
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22

      - name: Install dependencies
        run: npm ci

      - name: Fetch model assets
        run: |
          mkdir -p .cache/models .tmp/background-removal-data public/models/background-removal
          curl -L "https://staticimgly.com/@imgly/background-removal-data/1.7.0/package.tgz" -o ".cache/models/background-removal-data-1.7.0.tgz"
          tar -xzf ".cache/models/background-removal-data-1.7.0.tgz" -C ".tmp/background-removal-data"
          cp -R .tmp/background-removal-data/package/dist/* public/models/background-removal/

      - name: Build static app
        run: npm run build:github-pages

      - name: Disable Jekyll
        run: touch dist/.nojekyll

      - name: Configure Pages
        uses: actions/configure-pages@v6

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: dist

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
